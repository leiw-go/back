<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaowenltd.projectinfomationmanage.mapper.UserMapper">

    <resultMap id="UserResultMap" type="User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="BaseColumns">
        id, username, password, real_name, email, phone, status, create_time, update_time
    </sql>

    <insert id="insertUser" parameterType="User">
        INSERT INTO t_user (id, username, password, real_name, email, phone, status, create_time, update_time)
        VALUES (#{id}, #{username}, #{password}, #{realName}, #{email}, #{phone}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE t_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteUserById">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

    <select id="findUserById" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/> FROM t_user WHERE id = #{id}
    </select>

    <select id="findUserByUsername" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/> FROM t_user WHERE username = #{username}
    </select>

    <select id="findAllUsers" resultMap="UserResultMap">
        SELECT <include refid="BaseColumns"/> FROM t_user ORDER BY create_time DESC
    </select>

</mapper>
