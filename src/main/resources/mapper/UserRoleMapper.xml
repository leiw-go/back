<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaowenltd.projectinfomationmanage.mapper.UserRoleMapper">

    <resultMap id="UserRoleResultMap" type="UserRole">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="role_id" property="roleId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insertUserRole" parameterType="UserRole">
        INSERT INTO t_user_role (id, user_id, role_id, create_time)
        VALUES (#{id}, #{userId}, #{roleId}, #{createTime})
    </insert>

    <delete id="deleteUserRoleByUserId">
        DELETE FROM t_user_role WHERE user_id = #{userId}
    </delete>

    <delete id="deleteUserRoleByRoleId">
        DELETE FROM t_user_role WHERE role_id = #{roleId}
    </delete>

    <select id="findUserRolesByUserId" resultMap="UserRoleResultMap">
        SELECT id, user_id, role_id, create_time FROM t_user_role WHERE user_id = #{userId}
    </select>

</mapper>
